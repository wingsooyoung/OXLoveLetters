<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>

    <style>
      {% include "css/styles.css" ignore missing %}
    </style>

    <script src="https://unpkg.com/phosphor-icons"></script>

</head>

<body>
<header>
    <nav>
        <div class="dropdown">
            <a href="/">HOME</a>

            {%- for page in collections.pages -%}
                {% if page.data.title == 'Write Letters' %}
                    <a href="{{ page.url }}">WRITE LETTERS</a>
                {% elseif page.data.title == 'Read Letters' %}
                    <a href="{{ page.url }}">READ LETTERS</a>
                {% endif %}
            {%- endfor -%}

            <a href="/" class="about">ABOUT <i class="ph-caret-down"></i></a>

            <div class="dropdown-content">
                {%- for page in collections.pages -%}
                    {% if page.data.title == 'About OMEGA X' %}
                    <a href="{{ page.url }}">ABOUT OMEGA X</a>
                    {% elseif page.data.title == 'About This Project' %}
                    <a href="{{ page.url }}">ABOUT THIS SITE</a>
                    {% endif %}
                {%- endfor -%}
            </div>
        </div>
    </nav>
</header>


    {{ content | safe }}


    {% footer %}

    {% set js %}
      {% include "js/main.js" %}
    {% endset %}
    <script>
      {{ js | safe }}
    </script>

</body>
</html>



